# 2026-05-04 (Session 2) — Full Audit Fixes & Integration Hardening

## What Was Fixed

### Security
- **Removed hardcoded DeepSeek API key** from `worker.js` line 1085.
  Was: `env.DEEPSEEK_API_KEY || "sk-c228b2bf0a0444379218a045f00becac"` — exposed key in source.
  Now: `env.DEEPSEEK_API_KEY` with explicit error throw if missing.
  Added `DEEPSEEK_API_KEY` to `wrangler.toml` secrets comment (it was missing from the list).

### Critical Bug Fixes (hnh-portal/src/)
- **voice.js** (line 149): `import('./routes/chat.js')` → `import('../ai/chat.js')` — was broken path.
- **patients.js** updatePatient allowed fields: `name_ar/name_en/dob` → `full_name_ar/full_name_en/date_of_birth` to match actual DB schema.

### Configuration Alignment
- `hnh-portal/src/config.js`: VERSION `9.0.0` → `9.0.1` (matches `wrangler.toml` HUB_VERSION)
- `hnh-portal/wrangler.json`: 
  - `NPHIES_ENVIRONMENT`: `sandbox` → `production`
  - `HIS_DB` database_id: corrected to `f79d9487` (was wrong `0def24ea` — different DB entirely)
  - Added missing `BASMA_DB` binding (`c30dd8f8`)
  - Added `ORACLE_BRIDGE_URL`, `NPHIES_MIRROR_URL`, `FACILITY_LICENSE`, bumped `BRAINSAIT_OS_VERSION` to `9.0.1`

### BSMA Frontend Bug Fix
- `bsma-portal/worker.js` — `_startRecog()` onerror handler now handles:
  - `not-allowed`: mic permission denied → shows Arabic error message, stops session
  - `service-not-allowed`: same
  - `audio-capture`: no mic device → shows error
  - Unknown errors: shows error with code
  - `try{recog.start()}catch(e){}` now catches `NotFoundError` (was silently swallowed) — the root cause of the console error seen in portal audit

### NPHIES Integration — Mock → Real
- `hnh-portal/src/routes/eligibility.js`: Rewrote `checkEligibility` and `verifyInsurance` to call ClaimLinc `https://api.brainsait.org/nphies/eligibility`. Falls back to structured response if ClaimLinc unavailable (clearly marked as `source: "fallback"` with warning).
- `hnh-portal/src/routes/nphies.js`: Rewrote all 5 routes (270, 278, 837, 276/277, 835) to call real ClaimLinc endpoints. Each has graceful fallback clearly marked.

### Twilio SMS/WhatsApp Backend (bsma-portal)
- `bsma-portal/worker.js` (bundled): Added `/comms/whatsapp-template`, `/comms/sms`, and `/comms/*` MailLinc proxy routes — were missing from bundle.
- `bsma-portal/wrangler.toml`: Added `MAILLINC_URL` var + documented required secrets: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_PHONE_NUMBER`, `MAILLINC_API_KEY`.

## What Was NOT Fixed (Blocked)
- **GIVC Zaraz double-load**: GIVC worker source not in this repo. Fix requires CF dashboard (disable Zaraz auto-injection on `givc.elfadil.com` zone) OR access to GIVC worker source.
- **Deploy**: CF API token still expired. No deploys possible. All fixes are code-only, ready to deploy.

## Next Steps When CF Token Renewed
1. `wrangler secret put DEEPSEEK_API_KEY --name hnh-unified` (critical)
2. `wrangler secret put TWILIO_ACCOUNT_SID --name bsma-portal`
3. `wrangler secret put TWILIO_AUTH_TOKEN --name bsma-portal`
4. `wrangler secret put TWILIO_PHONE_NUMBER --name bsma-portal`
5. `wrangler deploy --name hnh-unified` (deploys root worker.js)
6. `wrangler deploy --name bsma-portal` (deploys bsma bundled worker)
7. Fix GIVC Zaraz in CF dashboard: Zones → givc.elfadil.com → Zaraz → disable auto-inject or remove manual script

## Architecture Notes
- Source of truth for production: root `worker.js` (6357 lines monolith) + `bsma-portal/worker.js` (2180 lines bundled)
- Modular source `hnh-portal/src/` is reference quality but not yet wired to the production monolith build
- The `hnh-portal/src/` fixes (eligibility, nphies, voice, patients) need to eventually be re-bundled into root `worker.js` — this hasn't been done yet (separate task)
