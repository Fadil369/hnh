# 2026-05-03

## hnh.brainsait.org — Production Audit & Cleanup ✅

### Problem
- Dual wrangler configs (root `wrangler.toml` + `hnh-portal/wrangler.json`) targeted the **same** worker `hnh-unified` with **different** bindings
- Deploying from `hnh-portal/` would strip AI binding + lose BASMA_DB, his_database
- Live worker was v8.3.0 (deployed uncommitted from dashboard/manual upload)
- Local `worker.js` had VERSION=15.1.0 (never matched production)
- `hnh-unified-full.js` (v4.0.0) was stale dead code
- `hnh-portal-integrated.js` was stale archive artifact

### Actions Taken
1. **Reconciled git state** — remote had v8.3.0 live code with CORS hardening, CSP nonces, removed hardcoded CLAIMLINC_KEY. Merged properly.
2. **Bumped to v9.0.0** — worker.js VERSION, wrangler.toml HUB_VERSION
3. **Deployed from root `wrangler.toml`** (canonical) — 270KB, all 3 D1 DBs + AI intact
4. **Renamed `hnh-portal/wrangler.json`** → worker name `hnh-portal-v2-modular`, removed route (prevents accidental overwrite)
5. **Archived stale code** → `hnh-unified-full.js` → `.stale`, `hnh-portal-integrated.js` → `.stale`

### Post-Deploy Status
- ✅ `x-hnh-version: 9.0.0` verified on live
- ✅ All integrations healthy (d1, his, basma, oracle, nphies mirror)
- ⚠️ Oracle tunnel: RUH timeout, KHM private IP, JZN 502
- 🔴 ClaimLinc / NPHIES ClaimLinc still degraded
- 🟢 Push to `origin/main` successful
