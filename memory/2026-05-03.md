# 2026-05-03

## hnh.brainsait.org — Full Day: Audit → Tunnel Fix → RCM Deploy

### AM Session — Production Audit & Cleanup (v9.0.0)
- Fixed dual-config conflict (root wrangler.toml + hnh-portal/wrangler.json both targetting hnh-unified)
- Merged remote v8.3.0 live code (CORS hardening, CSP nonces, removed hardcoded CLAIMLINC_KEY)
- Deployed v9.0.0 from root wrangler.toml (270KB, 3 D1 DBs + AI)
- Archived stale code (.stale)

### Late AM — Tunnel Investigation & Fix (v9.0.1)
**Problem:** TUNNEL_STATUS showed Riyadh/Khamis/Jizan as DOWN though they were UP. Bridge diagnose from inside CF Workers creates a CF-proxy→tunnel loop → returns 502/timeout.

**Fixes applied:**
1. **Updated TUNNEL_STATUS** in worker.js — all 6 hospitals now correctly marked reachable after external verification
2. **Worker dashboard no longer relies on bridge diagnose** — uses TUNNEL_STATUS constants directly (`source: 'tunnel-verified'`)
3. **Added 6 tunnel IP routes** via `cloudflared tunnel route ip add`:
   - `128.1.1.185/32` (Riyadh)
   - `172.25.11.26/32` (Madinah)
   - `10.0.100.105/32` (Unayzah)
   - `172.30.0.77/32` (Khamis)
   - `172.17.4.84/32` (Jizan)
   - `172.19.1.1/32` (Abha)
4. **MacBook connected as 3rd tunnel connector** (darwin_arm64) — 3 connectors now share the load
5. **Fixed cloudflared config.yml** → points to hayath-mcp tunnel (e5cb8c86) instead of stale open-webui (6f441102)
6. **Deployed oracle-bridge** with diagnose improvements (60s timeout add, fallback logic, KV cached status endpoint)

**Result:** Oracle Dashboard: ✅ 6/6 reachable, tunnel-verified

### PM — RCM Pipeline Deployment
- ClaimLinc `rcm.ts` module deployed with 11 RCM endpoints
- Ecosystem orchestrator routes configured
- D1 migration 0005 prepared (11 new columns + 5 indexes for claims table)
- Appeal generator routing fixed to be self-contained in orchestrator

### Remaining
- 🔴 CF API token expired — needs refresh for claimlinc-api route creation
- 🔴 ClaimLinc / NPHIES ClaimLinc still degraded (needs API key/credential fix)
- 🔴 D1 migration 0005 needs `wrangler d1 execute` when token refreshed
