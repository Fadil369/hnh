# 2026-05-05 — HNH pages.js IIFE Fix + Server-Side Grid Pre-Render

## Root Cause (resolved)
The `scr()` function in `hnh-portal/src/pages.js` was injecting `var DE/BR/DO` JSON arrays
into the browser and building DOM grids client-side via `rd()`, `rb()`, `rdo()`.
The `.map()` callbacks used `\"` inside single-quoted source strings — which produces literal `"`
in the output script — breaking string delimiters mid-expression → `SyntaxError`, IIFE crash.
Same bug in `loadIntegrations()` html+= line and nphies_mirror stats inject.

## What Was Fixed

### hnh-portal/src/pages.js
1. **`hdr()` book button**: `href="tel:+p"` → `href="https://bsma.elfadil.com" target="_blank"`
   (Header "Book Appointment" now opens BSMA portal directly instead of dialing)

2. **`scr()` full rewrite** (server-side pre-render strategy):
   - Removed `var DE=...`, `var BR=...`, `var DO=...` JSON injections
   - Removed `rd()`, `rb()`, `rdo()` functions and their `rd();rb();rdo();` init calls
   - Fixed `loadIntegrations()`:
     - Error/offline innerHTML: `\"` → `\'` pattern (HTML attrs in single-quoted JS strings)
     - `html+=` forEach line: rebuilt as intermediate vars (`cls`, `scls`, `stxt`, `tnl`)
       then `html+=\'<div class="\'+cls+\'">...\'` — valid, no ambiguous quote nesting
     - `nphies_mirror` stats inject: same `\'...\'+expr+\'...\'` fix
   - `doSearch()` was already correct (used `\'` for HTML attrs) — left unchanged
   - All other functions (scroll, lang toggle, IntersectionObserver, stats) unchanged

3. **`servePage()` — server-side grid rendering**:
   - Added `dgHtml`, `bgHtml`, `dogHtml` pre-render vars (pure server-side JS, no quoting issues)
   - Sections now inject HTML directly: `<div class="g4" id="dg">'+dgHtml+'</div>`
   - Instant first-paint for departments, branches, doctors — no JS required

## Build & Deploy
- `npx esbuild hnh-portal/src/index.js --bundle --format=esm --target=esnext --platform=browser --outfile=worker.js --minify --keep-names`
- `npx wrangler deploy`
- **Deployed version**: `9349649b-d845-4c18-92dd-8fa80bdc8858` — 618.35 KB

## Smoke Tests (all pass)
- `القلب والأوعية` / `Cardiology` present in HTML → departments pre-rendered ✓
- `المدينة المنورة` / `Madinah` present → branches pre-rendered ✓
- `د. معاوية دبورة` / `Dr. Muawiya Daboura` present → doctors pre-rendered ✓
- `var DE=` absent in served HTML → no client-side array injection ✓
- Header navBook: `href="https://bsma.elfadil.com" target="_blank"` ✓
- `doSearch`, `loadIntegrations`, `srchQ` all present ✓

## New Routes Also Live (from prior session, now deployed)
- `/api/search` — AutoRAG AI search (search.js)
- `/api/homecare/*` — Home care visits, nurses, vitals (homecare.js)
- `/api/telehealth/*` — Sessions, prescriptions, availability (telehealth.js)
- `/api/email/*` — 4 email templates + audit log via MailLinc (email.js)
- D1 migration 0006 applied — 28 tables total including homecare & telehealth

## HNH Provider Dashboard + GIVC/FHIR Deploy
- Added live premium provider dashboard renderer in `hnh-portal/src/pages.js`.
- Routed both `/provider-dashboard` and `/doctor-dashboard` through `servePage()`.
- Homepage provider ecosystem tile now links to `/provider-dashboard`.
- Dashboard pulls live data from `/api/stats`, `/api/providers`, `/api/appointments`, `/api/patients`, `/api/health`, and `/givc/api/providers/givc-network`.
- Dashboard eligibility action links to the GIVC eligibility UI (`/givc?lang=<lang>#eligibility`) instead of dead-linking to POST-only `/api/nphies/270`.
- Provider source cleanup in `routes/providers.js`: DB-backed names strip existing `Dr.` / `د.` prefixes before formatting, preventing `Dr. Dr.` and `د. د.`.
- FHIR Practitioner now emits GIVC OID identifiers and GIVC/branch extensions.
- Rebuilt `worker.js` with esbuild and deployed `hnh-unified` version `9d325a6b-55a1-4efd-be0e-31eec080a23c`.
- Verified live GET routes: `/provider-dashboard?provider=DRV-S85MNP` and `/doctor-dashboard` return 200 HTML.
- Verified live provider lookup: `/api/providers/DRV-S85MNP` returns provider `272`, OID `1.3.6.1.4.1.61026.7.1777961138677388`, no double prefix.
- Verified live FHIR: `/api/fhir/Practitioner/DRV-S85MNP` returns `urn:oid:1.3.6.1.4.1.61026.7.1777961138677388`.
- Verified `/api/health`: DB/HIS connected; ClaimLinc/GIVC/Basma/SBS/NPHIES mirror connected; Oracle Bridge warning persists intermittently.
- Note: `curl -I` returns 404 for dashboard pages because router only handles GET, not HEAD. Browser-style GET is OK.

## Remaining Route Fixes
- Fixed HNH router `HEAD` handling in `hnh-portal/src/router.js`: `HEAD` now matches `GET` routes and returns the same status/headers with an empty body.
- Rebuilt and deployed HNH worker version `69561f23-d0c9-4eab-b5db-3d6dc2914314`.
- Fixed provider dashboard eligibility action to use `/givc/?lang=<lang>#eligibility` so it reliably matches the GIVC wildcard route.
- Fixed GIVC dashboard to expose `id="eligibility"` on the actual eligibility card and added `HEAD` handling for GIVC HTML/JSON GET routes.
- Deployed GIVC worker version `3bb2f255-51a9-4b4c-a9c0-92adaa396507`.
- Verified live `curl -I` now returns 200 for `/provider-dashboard`, `/doctor-dashboard`, `/givc/?lang=en`, and `/givc/api/providers/givc-network`.
- Verified live `/api/health` now reports Oracle Bridge, NPHIES mirror, ClaimLinc, Basma, SBS, and GIVC all connected at time of check.
- Confirmed direct Oracle Bridge `/health` is 200; earlier Oracle warning was transient rather than a bad HNH route.

## HNH Homepage UI Enhancement Deploy
- Enhanced `hnh-portal/src/pages.js` homepage with a new live command center section (`#command`) and new feature launch rail (`#features`).
- Added bilingual Arabic/English labels for command center metrics and new feature descriptions.
- New command center pulls live counters from `/api/stats`, `/api/homecare/stats`, `/api/telehealth/stats`, `/givc/api/providers/givc-network`, and `/api/health`.
- Metrics shown: patient records, today visits, providers, claims, home visits, telehealth sessions, GIVC doctors, connected systems.
- Feature launch rail links/actions: doctor dashboard, GIVC eligibility, home healthcare modal, telehealth modal, AI search, RCM modal.
- Added `ENHANCE_CSS` for dark command-center visual treatment and feature cards while preserving existing HNH visual language.
- Rebuilt and deployed HNH worker version `629ef4b1-fa5f-4271-8261-05b776ee7021`.
- Verified live `/?lang=en` and `/?lang=ar` include `#command`, `#features`, `loadCommand()`, provider dashboard link, and GIVC eligibility link.
- Verified backing stats APIs: `/api/homecare/stats` and `/api/telehealth/stats` return live counts; `/api/health` reports integrations connected.
