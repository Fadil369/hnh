# Memory — 2026-05-02

## BSMA Portal v3.0 — Major Upgrade Deployed

### Summary
Created and deployed a new **standalone BSMA Portal Worker** (`bsma-portal`) at `bsma.elfadil.com/*`, replacing the old embedded UI in `brainsait-healthcare-gateway`. The worker serves the v3.0 BSMA AI healthcare assistant as an independent frontend.

### What Changed
- **Old architecture:** BSMA portal was embedded as a raw HTML string inside `brainsait-healthcare-gateway` worker (104KB monolithic script). Shared the worker with other routes (`healthcare.elfadil.com`, `health.elfadil.com`).
- **New architecture:** Separate `bsma-portal` worker with dedicated project at `~/bsma-portal/`. Clean separation of concerns:
  - `src/worker.js` (13.7KB) — Proxy logic for 12+ API endpoints + security headers
  - `src/html.js` (43KB) — v3.0 BSMA HTML as importable module
  - `wrangler.toml` — Route: `bsma.elfadil.com/*`, env vars for backends

### API Endpoints Supported (same as old gateway)
| Route | Backend | Description |
|-------|---------|-------------|
| `/basma/chat` | HNH `/api/assistant` | AI healthcare chat (DeepSeek) |
| `/basma/tts` | ElevenLabs API | Text-to-Speech (Arabic: Salma/Saudi, English: Sarah) |
| `/basma/network` | ClaimLinc `nphies/network/summary` | Financial network data (835M SAR, 98.6% approval) |
| `/basma/mirror` | NPHIES Mirror Worker | NPHIES sync status (81 GSS, 51K PA) |
| `/basma/session` | Worker | New session UUID |
| `/basma/hospitals` | Worker | 6 hospital list |
| `/basma/crm` | HNH proxy | Patient CRM data |
| `/basma/drugs` → `/basma/oracle` → `/basma/moh` → `/basma/rag/*` | Voice Agent proxy | Drug search, Oracle FHIR, MOH, RAG |
| `/api/*` | HNH proxy | All HNH API endpoints (hnh.brainsait.org) |

### Security
- CSP: locked connect-src to whitelist (hnh.brainsait.org, api.brainsait.org, voice.elfadil.com, nphies-mirror)
- HSTS: max-age=31536000, includeSubDomains, preload
- X-Frame-Options: DENY
- X-Content-Type-Options: nosniff
- Permissions-Policy: microphone=self only
- CORS: hardcoded to `https://bsma.elfadil.com`

### File Locations
- Worker project: `~/bsma-portal/` (wrangler.toml, src/worker.js, src/html.js)
- BSMA v3.0 HTML source: `~/hnh-portal/static/bsma.html` (42KB, 22/22 validation checks passed)
- Old gateway: `brainsait-healthcare-gateway` still serves `healthcare.elfadil.com` and `health.elfadil.com`

### Notes
- NPHIES Mirror endpoint (`/basma/mirror`) has intermittent timeout issue (pre-existing)
- ClaimLinc `/basma/network` requires `X-API-Key` header for full access (currently working with empty key due to public endpoint)
- Worker compiled: 81.79KB uncompressed / 19.55KB gzip
- Version: 3.0.0
- Route removed from old gateway (`brainsait-healthcare-gateway`) before deployment
# Memory — 2026-05-02

## BSMA Portal v3.0 — Major Upgrade Deployed

### Summary
Created and deployed a new **standalone BSMA Portal Worker** (`bsma-portal`) at `bsma.elfadil.com/*`, replacing the old embedded UI in `brainsait-healthcare-gateway`. The worker serves the v3.0 BSMA AI healthcare assistant as an independent frontend.

### What Changed
- **Old architecture:** BSMA portal was embedded as a raw HTML string inside `brainsait-healthcare-gateway` worker (104KB monolithic script). Shared the worker with other routes (`healthcare.elfadil.com`, `health.elfadil.com`).
- **New architecture:** Separate `bsma-portal` worker with dedicated project at `~/bsma-portal/`. Clean separation of concerns:
  - `src/worker.js` (13.7KB) — Proxy logic for 12+ API endpoints + security headers
  - `src/html.js` (43KB) — v3.0 BSMA HTML as importable module
  - `wrangler.toml` — Route: `bsma.elfadil.com/*`, env vars for backends

### API Endpoints Supported (same as old gateway)
| Route | Backend | Description |
|-------|---------|-------------|
| `/basma/chat` | HNH `/api/assistant` | AI healthcare chat (DeepSeek) |
| `/basma/tts` | ElevenLabs API | Text-to-Speech (Arabic: Salma/Saudi, English: Sarah) |
| `/basma/network` | ClaimLinc `nphies/network/summary` | Financial network data (835M SAR, 98.6% approval) |
| `/basma/mirror` | NPHIES Mirror Worker | NPHIES sync status (81 GSS, 51K PA) |
| `/basma/session` | Worker | New session UUID |
| `/basma/hospitals` | Worker | 6 hospital list |
| `/basma/crm` | HNH proxy | Patient CRM data |
| `/basma/drugs` → `/basma/oracle` → `/basma/moh` → `/basma/rag/*` | Voice Agent proxy | Drug search, Oracle FHIR, MOH, RAG |
| `/api/*` | HNH proxy | All HNH API endpoints (hnh.brainsait.org) |

### Security
- CSP: locked connect-src to whitelist (hnh.brainsait.org, api.brainsait.org, voice.elfadil.com, nphies-mirror)
- HSTS: max-age=31536000, includeSubDomains, preload
- X-Frame-Options: DENY
- X-Content-Type-Options: nosniff
- Permissions-Policy: microphone=self only
- CORS: hardcoded to `https://bsma.elfadil.com`

### File Locations
- Worker project: `~/bsma-portal/` (wrangler.toml, src/worker.js, src/html.js)
- BSMA v3.0 HTML source: `~/hnh-portal/static/bsma.html` (42KB, 22/22 validation checks passed)
- Old gateway: `brainsait-healthcare-gateway` still serves `healthcare.elfadil.com` and `health.elfadil.com`

### Notes
- NPHIES Mirror endpoint (`/basma/mirror`) has intermittent timeout issue (pre-existing)
- ClaimLinc `/basma/network` requires `X-API-Key` header for full access (currently working with empty key due to public endpoint)
- Worker compiled: 81.79KB uncompressed / 19.55KB gzip
- Version: 3.0.0
- Route removed from old gateway (`brainsait-healthcare-gateway`) before deployment

---

## Later — DeepSeek Engine Integration
- Added DeepSeek API key `sk-c228b2bf0a0444379218a045f00becac` as primary AI engine (model: `deepseek-v4-flash`)
- Fallback chain: DeepSeek → Claude Sonnet 4 → CF Workers AI LLaMA 3.3 70B → static fallback
- Deployed to 3 workers: `basma-voice-agent b9f27b81`, `hnh-unified eff90545`, `brainsait-healthcare-gateway`
- Full bilingual support: Arabic and English tested live

## Oracle Credentials Update
- Updated to `U29200` / `U29201` across all workers
- Set as JSON creds per hospital (Riyadh, Madinah, Unaizah, Khamis, Jizan, Abha) on `oracle-bridge`
- All 6 workers got `ORACLE_USER` + `ORACLE_PASSWORD` secrets
- `hnh-unified` deployed as v7.3.0

## HNH Portal Hub — Integrated Ecosystem Section (v8.0)
- Built **ecosystem section** directly into `hnh-portal/src/pages.js`:
  - 7 role-based portal cards (Patient, Provider, Payer, Student, Admin, Reception, RCM) + BSMA link — 8 cards total
  - Live integration status grid (6 systems: Oracle, NPHIES, ClaimLinc, BSMA, SBS, GIVC) via `/api/health`
  - AI assistant widget with voice orb, Arabic/English toggle
  - RTL support, responsive design, intersection observer animations
  - Embedded NPHIES stats (81 GSS, 51,297 PA)
  - `loadIntegrations()` function fetches health data and renders color-coded badges
- Fixed unterminated string literal in `pages.js` (CSS double quotes → single quotes, + closing `"` on CSS string)
- Added `/api/health` endpoint returning `{success: true, integrations: {...}}` with all 6 systems + NPHIES stats
- Fixed health route in `index.js` — was returning inline static data instead of calling `routes/health.js`
- Added `/api/portal/integrations` aggregate endpoint
- Workers deployed: `hnh-unified` (versions across multiple deployments: `a73b81c2`, `f8028c94`, `159ec1d3`, `bdd66257`)
- Live verification: all endpoints returning 200, integrations status visible on homepage

## Pull Request #7 — Merged & Deployed (v8.5)
- Merged PR #7 (Fadil369/hnh#7) from `copilot/transform-landing-page-to-healthcare-portal` branch
- PR added 26 static HTML pages + site/ folder + monolithic `worker.js` (311KB) + `frontend/` Next.js app
- **Strategy:** Kept our modular `index.js` + `pages.js` architecture (not the monolithic worker.js)
- Imported all 26 static pages as JS string modules in `constants/site.js` (447KB total, 3 files)
- Added route checking in `servePage()` — serves static pages at these paths:
  - `/about`, `/academy`, `/blog`, `/faq`, `/packages`, `/branches`, `/contact`, `/departments`, `/doctors`
  - `/course-*` (9 courses): NPHIES, Medical Coding, RCM, AI in Healthcare, PDPL, Clinical Document, Pharmacy, CBAHI, Infection Control
  - `/index` (but main `/` always serves premium ecosystem homepage)
- Premium ecosystem homepage **preserved** — 29,477 bytes, all features intact
- Static pages include SEO improvements from PR: schema.org JSON-LD, hreflang tags, canonical URLs, Open Graph, Twitter Cards
- Total worker size: ~629KB (well under 1MB limit ✅)
- Worker version: `5ba42f3b-0d30-466d-86ee-7d1dbcaf27a6`
- Git: PR merged with 2 parents `295aba7` + `9d0e4f4`, commit `b6cfa9c`

### AI Engine Architecture (Current)
```
User Input
  ↓
1. 🔷 DeepSeek deepseek-v4-flash ← PRIMARY
   sk-c228b2bf0a0444379218a045f00becac
  ↓ (fallback)
2. 🤖 Claude Sonnet 4 ← Secondary
  ↓ (fallback)
3. ⚡ CF Workers AI LLaMA 3.3 70B ← Tertiary
  ↓ (fallback)
4. 💬 Static Arabic/English reply ← Hard fallback
```

### Voice Agent (Basma v2.0)
- **ElevenLabs key:** `af319126588134029bb0150cfd2c7196bfcee5190fe2a5ed0ef28a6253cda47e`
- Arabic voice: `KxMRrXEjbJ6kZ93yT3fq` (Salma, warm female)
- English voice: `21m00Tcm4TlvDq8ikWAM` (Rachel)
- Arabic model: `eleven_multilingual_v2`, English: `eleven_monolingual_v1`
- Quality: Stability 0.7, Similarity 0.85, Style 0.2
- Voice agent live at `voice.hnh.brainsait.org` and `basma.hnh.brainsait.org`
- Embed script: `<script src="https://voice.hnh.brainsait.org/voice/embed">`

### Portal Hub Architecture
```
hnh.brainsait.org/
├── (premium ecosystem homepage — 8 cards + integrations + AI widget)
├── /about, /academy, /blog, /faq, /packages, /branches, /contact, /departments, /doctors
├── /course-* (9 academy courses)
├── /api/health → integrations status
├── /api/portal/integrations → aggregated portal data
├── /api/chat → DeepSeek AI
├── /api/nphies/analysis → NPHIES analytics
├── /api/academy/courses → course data
└── /api/blog → articles
```

### Key Decisions
1. **Standalone BSMA Worker** — Extraction from monolithic gateway for clean separation
2. **Healthcare context for chat** — Changed from Mellissa hotel API to HNH `/api/assistant` (DeepSeek)
3. **Modular over monolithic** — Kept `index.js` + `pages.js` + `routes/` structure instead of adopting PR#7's monolithic `worker.js`
4. **CSS as module string** — Fixed unterminated string issues by switching to single-quote delimiters
5. **Static pages as JS modules** — Imported 26 HTML pages as string constants rather than serving from external folder
6. **DeepSeek as primary** — Faster response time and excellent Arabic support compared to Claude fallback

### Remaining Issues
- NPHIES Mirror endpoint intermittent timeout (pre-existing backend issue)
- ClaimLinc `/basma/network` requires `X-API-Key` for full enterprise access
- `/api/health` sometimes times out when checking all 6 integrations simultaneously
- Oracle bridge status shows "degraded" because `/health` endpoint not exposed at expected URL
- No rate limiting implemented on BSMA portal
- No git repo for `bsma-portal` project (suggested to init)
